#include "Earth Tanks.h"

earthTanks::earthTanks(int ID, int attackCap, int joinedTime, int bTime, int p, double h)
	:earthUnit(id, attackCapacity, joinTime, battleTime, power, health) {}

//void earthTanks::print() {
//	std::cout << "Health of tank = " << getHealth();
//	std::cout << "\nPower of tank = " << getPower();
//	std::cout << "\nMaximum attack capacity of tank = " << getAttackCapacity();
//	std::cout << "\nID of tank = " << getID();
//	std::cout << "\nJoin time of tank = " << getJoinTime() << std::endl;
//}